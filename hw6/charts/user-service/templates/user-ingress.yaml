apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: app
spec:
  rules:
  - host: arch.homework
    http:
      paths:
      - backend:
          serviceName: user-service
          servicePort: 8000
        path: /users/me
